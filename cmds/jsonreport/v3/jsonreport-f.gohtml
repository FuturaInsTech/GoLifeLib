<!DOCTYPE html>
<html>
<head>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      font-size: 10pt;
      font-family: Arial, sans-serif;
      text-align: left;
    }
    .footer {
      border-top: 1px solid #000;
	  padding-top: 4px; /* instead of 2px */
      margin-top: 10px;  /* adds extra gap above wkhtmltopdf's number */
    }
    h1 { color: navy; font-size: 22px; text-align: center; }
    h2, h3, h5 { color: black; text-align: center; }
    h2 { font-size: 16px; }
    h3 { font-size: 11px; }
    h4 { color: black; font-size: 12px; text-align: right; }
	h5 { color: black; font-size:8px; position: absolute; top: 0; left: 0;  padding-top: 10px; }
    .logo {
      max-height: 20px; /* adjust size as needed */
      margin-bottom: 1px;
    }
  </style>
</head>
<body>
  <div class="footer">
    {{if .LogoPath}}
      <div><img class="logo" src="{{.LogoPath}}" style="max-height:20px; max-width:50px; text-align: left;";></div>
    {{end}}
	 <div><h5>{{.CoName}}</h5></div>
   </div>
</body>
</html>
